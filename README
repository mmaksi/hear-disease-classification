You can recreate the same environemnt on your machine by running this command:

`conda env create -f environment.yml`

Tools used in this project:

- Pandas
- NumPy
- Matplotlib
- Scikit-Learn
- Jupyter Notebook
